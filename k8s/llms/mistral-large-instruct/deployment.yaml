apiVersion: apps/v1
kind: Deployment
metadata:
  name: mistral-large-instruct
  namespace: bdarabisahneh
  labels:
    app: mistral-large-instruct
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mistral-large-instruct
  template:
    metadata:
      labels:
        app: mistral-large-instruct
    spec:
      volumes:
      - name: cache-volume
        persistentVolumeClaim:
          claimName: vllm-cache
      # vLLM needs to access the host's shared memory for tensor parallel inference.
      - name: shm
        emptyDir:
          medium: Memory
          sizeLimit: "2Gi"
      nodeSelector:
        gpu: a100
      containers:
      - name: mistral-large-instruct
        image: vllm/vllm-openai:latest
        command: ["/bin/sh", "-c"]
        args: [
          "vllm serve mistralai/Mistral-Large-Instruct-2411 --trust-remote-code --enable-chunked-prefill --tensor-parallel-size 8 --max_num_batched_tokens 1024 --max_model_len 12288"
        ]
        env:
        - name: HUGGING_FACE_HUB_TOKEN
          valueFrom:
            secretKeyRef:
              name: hugging-face-hub-token
              key: token
        ports:
        - containerPort: 8000
        resources:
          limits:
            cpu: "10"
            memory: 120G
            nvidia.com/gpu: "8"
          requests:
            cpu: "2"
            memory: 6G
            nvidia.com/gpu: "8"
        volumeMounts:
        - mountPath: /root/.cache/huggingface
          name: cache-volume
        - name: shm
          mountPath: /dev/shm
